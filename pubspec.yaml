name: saca_demo
description: SACA – Smart Adaptive Clinical Assistant (Whisper offline STT demo)
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Offline Whisper STT via whisper.cpp (Android + iOS)
  whisper_kit:
    path: third_party/whisper_kit

  # Sherpa-ONNX: used when deploying fine-tuned ONNX model later
  sherpa_onnx: ^1.13.0

  # Audio recording (16 kHz mono PCM)
  record: ^6.2.0
  path_provider: ^2.1.3
  shared_preferences: ^2.3.2

  # UI
  gap: ^3.0.1
  cupertino_icons: ^1.0.8
  window_manager: 0.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  plugin_platform_interface: ^2.1.8

flutter:
  uses-material-design: true
  assets:
    - assets/data/gurindji_lexicon.json
    - assets/models/
    - assets/models/saca-hybrid-logreg-v1/
    - assets/models/classifier-xgb-best/
    - assets/models/classifier-xgb-quick/
    - assets/models/whisper-gue-base-run4-rc1/
    - assets/models/sherpa-onnx-whisper-gue-base-run4-rc1/
    - assets/Images/
    
