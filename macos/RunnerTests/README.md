# macOS Runner Tests

## Purpose

This folder contains tests for the native macOS host shell.

## What Is Here

- XCTest files for macOS runner behavior.

## When To Edit This

- Add tests here when macOS-native behavior needs coverage.
- Keep shared Flutter tests in `test/`.

## Related Folders

- `macos/Runner/` contains the host code under test.
- `test/` contains cross-platform Flutter tests.
